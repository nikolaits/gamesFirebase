# GameFirebase - web project

## Introduction

1. Enter the project's folder - `cd gamesFirebase` 
2. Install the project's dependencies- `npm install`
3. Build and start the web project - `npm start`
